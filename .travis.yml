dist: xenial

language: node_js
node_js:
  - '12.16'

addons:
  chrome: stable
before_install:


cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - npm run test -watch false --no-progress -browsers ChromeHeadless